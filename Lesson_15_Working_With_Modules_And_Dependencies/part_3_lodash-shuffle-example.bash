npm install lodash
import _ from 'lodash';

const numbers = [1, 2, 3, 4, 5];
const shuffled = _.shuffle(numbers);
console.log(shuffled); // Outputs a shuffled array
npm install --save-dev @types/lodash
npm outdated
npm update lodash
